"use client"
import styles from './navmenu.module.css';
import Link from 'next/link';

const NavMenu = () => {
  return (
      <nav className={styles.navigation}>Navigation Bar</nav>
  );
}

export default NavMenu;